import Card from '../components/Card';

const Graph = () => {
  return (
    <Card title="Graph" description="Visualize graph nodes and edges in realtime.">
      <p className="text-sm text-text2">Graph canvas and filters can be mounted here.</p>
    </Card>
  );
};

export default Graph;

