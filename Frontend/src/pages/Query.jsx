import Card from '../components/Card';

const Query = () => {
  return (
    <Card title="Query" description="Run structured queries against your context graph.">
      <p className="text-sm text-text2">Query builder and result table go here.</p>
    </Card>
  );
};

export default Query;

